# Die Variablen werden vom Benutzer eingegeben oder mit Standardwerten gefüllt.
# Jede Variable wird im Abschnitt config.yaml gespeichert und an die spezifische Umgebung angepasst.

backup_dir: "/var/backup"

max_log_size: "10M"